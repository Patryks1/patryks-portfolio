import { INavButton } from '../interfaces';

const navButtons: INavButton[] = [
  {
    label: 'Server',
    path: '/'
  },
  {
    label: 'Static',
    path: '/static'
  }
];

export default navButtons;
