import { NextPage } from 'next';
import React from 'react';

const Home: NextPage = (): JSX.Element => {
  return (
    <React.Fragment>
      <section>
        <h1>CV</h1>
      </section>
    </React.Fragment>
  );
};

export default Home;
